name: Run tests

on:
  push:
    branches:
    - master
  pull_request_target:
    branches: [master, main]
    # using pull_request_target allows for running tests to access the repo's secrets

jobs:
  # code-test:
  #   name: Run tests on code
  #   uses: ./.github/workflows/run-code-tests.yaml
  
  # build-image:
  #   name: Build Docker image
  #   uses: ./.github/workflows/build-images.yaml
  #   needs: code-test
  
  webhook-tests:
    name: Run tests on webhooks
    uses: ./.github/workflows/run-webhook-tests.yaml
    # needs: build-image
